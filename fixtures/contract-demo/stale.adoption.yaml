# fixtures/contract-demo/stale.adoption.yaml — SELF-SMOKE FIXTURE, do not adopt.
# Intentionally and permanently stale: a0e3efb was the personal-world pin during
# the 2026-09-20 attestation gap; the library main has moved past it (44ec8f4)
# and will never go back. The self-smoke "contract freshness" demo asserts that
# `contractctl freshness` exits NON-ZERO (BEHIND) on this manifest — proof the
# harness's red path is genuinely red. See reusable-contract-freshness.yml.
schema: play-nice/adoption-v1
project: ci-harness-fixture-stale
source:
  repository: Rylee-Bee/play-nice-contracts
  revision: a0e3efbee315aeaa16910818e234bff3853417a6
always:
  - assume-unknown
