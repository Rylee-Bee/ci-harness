# Self-smoke fixture: the dullest possible healthz server. nginx serves
# its default index at / so the reusable workflow's curl retry loop has a
# real endpoint to wait on. Tag pinned per pins/ACTIONS.md.
FROM nginx:1.30.1-alpine
